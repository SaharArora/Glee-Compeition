# Candidate records

Each candidate gets an immutable versioned subdirectory or manifest binding: route ID, exact
claim, commit SHA, data/artifact hashes, dependencies, configuration, seeds/sample size,
commands, logs, atomic outputs, and small certificate. Editing creates a new version.
