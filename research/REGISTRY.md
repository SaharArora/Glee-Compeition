# Research route registry

| Route ID | Exact claim or mechanism | Current owner | Exact remaining gap | Smallest known obstruction | Next decisive test | Dependencies | Input revisions and evidence links | Route state | Evidence maturity | Stall classification | Reopen-only-if condition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R0 | Bounded Model B: the already-frozen mixed-fold joint opponent sampler predicts held-out joint behavior better than conditional independence. | root | None for the frozen formulation; its required run is complete. | Joint dependence worsened energy in B/N on both axes and persuasion retention was .330520. | No further Model-B formulation is authorized. Freeze the negative certificate. | Frozen mixed-fold artifacts and validator. | Competition `docs/REGISTRY.md`; report SHA `1a86cac...acec`; `research/FAILED.md`. | closed | verifier-backed | evidence against mechanism | User explicitly reopens R0 with a materially new scope. |
| R1 | A single theory-plus-validated-residual economic core is the shared baseline for four forced research entrypoints. | root | No economic-core gap remains at this checkpoint; payoff remains prohibited by the absent language environment/production manifest. | All four arms now fail without the exact Model-C and support artifacts, verify their bytes, expose identical provenance, and reject a mismatched arm. | Preserve this contract; any environment or economic change requires a new version and full parity. | R0 negative; shipped theory anchor; accepted Model C/support; non-Model-B holdout marginal population/catalogue. | `research/EVIDENCE/WAVE4_BASELINE_CONTRACT.json`; artifact manifest SHA `620e0b10...538e`; Wave 3 commit `a1438d0878dd89fff7d71accc7ddc7faebac983b`. | frozen | self-audited plus independent R4 baseline sub-audit | downstream environment dependency | Shared core, artifact bytes, population/catalogue, or receiver contract changes. |
| R2 | The exact treatment is a model-relative persuasion-seller e-process against a fixed hash-locked Model-C reference. | root | No implementation gap for this label; empirical payoff is deliberately unmeasured. | The sharp distribution-free future conditional upper bound from training data is `1`, so a nontrivial population-valid extension is impossible without extra assumptions. Bargaining/negotiation remain callback-censored and persuasion buyer is a self-outcome. | None in this wave; report this scope if/when an authorized factorial environment exists. | R1 core; Model C SHA `9daec869...b82c`; no Model B. | `research/ROUTES/R2_POPULATION_CLAIM_SETTLEMENT.md`; `research/EVIDENCE/R2_POPULATION_BOUND_KILL_CHECK.json`; `2/2` tests. | settled | theorem-strength kill check; treatment still self-audited | population premise killed | A prospectively justified population conditional bound and multiplicity design; never Model B or heuristic `E_*`. |
| R3 | A fixed language treatment changes only eligible persuasion-seller text while preserving the shared economic action. | root | User selection of a text-responsive environment; current offline eligible language population is empty. | Candidate text is delivered but ignored by all frozen offline receiver policies. Wave 5A now supplies a generic contract/capability/cache harness but project evidence supports no named receiver. | User supplies the 13 frozen receiver/selection/adapter/capability inputs in `WAVE5A_RECEIVER_DECISION.md`; only then run a generic capability certificate under separate authorization. | R1 core; dedicated language capability; no e-process or Model-B dependency. | `research/ROUTES/R3_LANGUAGE_DECISION.md`; `research/ROUTES/WAVE5A_RECEIVER_DECISION.md`; `tests/test_controlled_receiver.py`. | decision_pending | offline infrastructure unit/synthetic tested; no causal outcome evidence | causal identification/user selection | Explicit treatment-blind receiver selection, exact contract, generic untouched probes, adapter, caps, and external-call authorization. |
| R4 | The four forced arms and report verifier fail closed unless exact paired identities, immutable eligibility, capabilities, artifacts, and a hash-authorized 3,600-row production contract reconstruct. | root | Intentional obstruction: exact receiver and 3,600-row breadth/replication factorization are unselected; both report and pre-outcome authorization hashes are `None`. | Wave 5A builds exact scenario/Model-C/support/policy/RNG/output admission infrastructure and a fresh auditor rejects the fixed-contract hostile matrix, but no production row root exists. | Select receiver plus Design A/B/C, freeze exact rows and both pins, then obtain a new fresh audit of the activated version before any outcome. | R1 baseline; revision-2 estimands; controlled receiver contract; no Model B. | Report SHA `23cdbe69...f270`; `glee_eval/experiments/preoutcome_manifest.py`; `research/ROUTES/WAVE5A_MANIFEST_DESIGNS.md`; `tests/test_preoutcome_manifest.py`. | blocked_by_exact_obstruction | verifier-backed synthetic/offline checkpoint; no outcome evidence | receiver/design/authorization | User selects receiver and factorization; exact row root and two pins freeze; fresh activated audit passes. |
| R5 | The unrestricted competition champion integrates only confirmed components and is independently live-safe. | root competition owner | Reconcile dirty competition branch, freeze champion revision, and resolve exact one-run leaderboard command/cap. | Research routes must not alter the champion or consume its live authorization. | Resume the separate competition audit now that Wave 1 is durably synthesized; do not wait on research significance to bank authorized volume. | Existing competition gates/confirmations; live strict-cap adapter. | Competition worktree and `docs/REGISTRY.md`. | paused | candidate | orchestration separation | Resume after the Wave-1 durable commit; never import unfrozen research treatments. |
| W5B-A | The current schema-v1 opponent approximation is measurably wrong on candidate-reached sequential behavior, justifying a separately validated Model A. | root | No Wave 5B audit gap; a bargaining-only next campaign is frozen but unlaunched. | Released and live bargaining offer distributions both have central-80% undercoverage in both roles; player-2 MAE is also above .08 in both sources. Negotiation/persuasion defects did not meet the same live replication/visibility gate. | Run the separately authorized bargaining-only campaign in `WAVE5B_ABCD_AND_MODEL_A_CAMPAIGN.md`, then require independent structural validation. | Operational schema-v1 population; 1,188,434 released events; 181 terminal-complete live games; no Model B. | `research/EVIDENCE/WAVE5B_MODEL_A_NECESSITY.json`; `research/ROUTES/WAVE5B_ABCD_AND_MODEL_A_CAMPAIGN.md`; checkpoint `research/EVIDENCE/WAVE5B_ARCHITECTURE_CHECKPOINT.json`, implementation `a26f953`. | campaign_frozen_unlaunched | candidate/self-audited architecture evidence | long campaign intentionally not started | Explicit authorization for the frozen 3–6 CPU-hour campaign; no automatic start. |
| W5B-P | The local percentile/public-formula shadow score predicts official leaderboard outcomes accurately enough for confident placement forecasts. | root | Exact per-game validation is unidentifiable from existing logs. | Zero batches have both exact agent commit and official per-game rating/adjustment; two count-reconciled batches show endpoint overprediction of about +56 B, +126 N and +70 P rating points. | Add exact commit and per-game official scoring capture, then run the frozen Jordan canary. | 181 terminal games and batch snapshots. | `research/EVIDENCE/WAVE5B_LEADERBOARD_PROXY.json`; `research/ROUTES/WAVE5B_LEADERBOARD_AND_CANARY.md`. | blocked | negative/partial-identification evidence | missing historical ground truth | New prospectively captured per-game official outputs and adjustment fields; never reverse-engineered labels. |
| W5B-S | Factorial00 is a shared economic backbone close enough to replace Jordan for the first live canary. | root | No promotion evidence or live adapter exists; selection is withheld. | Offline paired n=900 is +.04757 overall (+.15995 B, +.00895 N, -.02620 P), including -.05730 persuasion seller. | Preserve as architecture evidence; first canary uses Jordan after logging repair. | Frozen non-Model-B population/catalogue, Model C and support. | `research/EVIDENCE/WAVE5B_SHARED_BACKBONE/summary.json`. | candidate_not_selected | bounded offline diagnostic | family-specific regression and no competition gate | Independently validated competition gate and live-safe artifact injection. |
| W5B-C | Current Jordan can run a scientifically attributable bounded live canary. | root | Launcher must record exact commit/dirty digest and official per-game scoring/update capability. | Existing live runs prove operation but omit those fields, which made the proxy validation underidentified. | Implement and independently test only the logging contract; then seek separate authorization for exactly 100 games/family. | Jordan commit `bce5785`; strict-cap live adapter; no ambient artifacts. | `research/ROUTES/WAVE5B_LEADERBOARD_AND_CANARY.md`; `research/ROUTES/WAVE5B_DATA_FEEDBACK.md`. | operational_not_canary_ready | operational history, no attributable per-game score evidence | logging/provenance prerequisite | Exact logger fields pass tests and a clean candidate tuple is frozen; live authorization remains separate. |
| W5D-P | A prospectively sized paired 2x2 study can estimate eligible persuasion-seller e-process, language and interaction effects with correct clustering, Holm control and ITT retention. | Wave 5D Route 1 | A300 is the sole recommendation, but production activation remains blocked by missing base-stratum cluster enforcement and an unspecified receiver-failure-to-payoff rule. | A300 has at most 300 independent primary clusters, central-case MDE `.03411`, and cannot be described as powered for `.0100`; current report uses row-level variance. | Implement cluster-bound manifest/report inference and a treatment-blind failure-to-payoff rule, then fresh-audit before setting either pin. | Frozen Wave 4 estimands; Wave 5C receiver proposal; theory+Model-C arms; no Model B. | `research/EVIDENCE/WAVE5D_PAPER_DESIGN_AUDIT.json`; `research/ROUTES/WAVE5D_PAPER_METHODS.md`; `research/ROUTES/WAVE5D_STATISTICAL_ANALYSIS_PLAN.md`. | candidate_blocked_before_activation | prospective arithmetic and synthetic infrastructure only; no outcomes | verifier/statistical-contract prerequisites | Both blockers are materially implemented, exact A300 rows and both pins are frozen prospectively, and a fresh independent audit passes. |
| W5E-P | A300 with language as the single primary, SESOI `.035`, cluster-mean inference, and deterministic receiver-failure ITT continuation is the smallest evaluated design/order that can meet the central precision target. | Wave 5E Route P | Implementation is frozen for a fresh independent paper/adapter/security audit; protected OpenAI key and audit-GO document are absent; capability not run; exact A300 rows and both production pins remain unset. | A200's single-primary MDE `.03617` exceeds the `.035` SESOI; A300's is `.02953`. The e-process's actual crossing/action-change frequency is unknown and can require implausibly large conditional effects when rare. | Independently audit exact commit/source hashes. Only after GO and protected key, run the single 100-request/200-attempt/$1 capability route. A PASS still requires a new full-study authorization. | Frozen Wave 5C receiver contract; stdlib-only GPT-4.1 adapter; no Model A/B. | `research/ROUTES/WAVE5E_PAPER_ACTIVATION.md`; `research/ROUTES/WAVE5E_RECEIVER_CAPABILITY.md`; `research/EVIDENCE/WAVE5E_PAPER_ACTIVATION.json`. | implementation_frozen_pending_independent_audit | prospective preoutcome arithmetic and offline hostile tests only; no receiver/factorial outcomes | independent audit and credential gate | Exact audit GO binds the frozen commit/hashes, then the separately authorized capability route passes; exact A300 rows/pins require another authorization. |

## Wave 5A offline-preflight checkpoint

- Implementation commit: `179fd706d6df4d4e9770a21200e8ec3ec07a920f`; exact file hashes,
  tests, audit verdict, boundaries, and user decisions are frozen in
  `research/EVIDENCE/WAVE5A_OFFLINE_PREFLIGHT_CHECKPOINT.json`.
- Route A is complete at `research/TECHNICAL_HANDOFF_2X2.md`. It is researcher-facing and
  source-grounded; it does not promote an empirical treatment claim.
- Route B is an implementation checkpoint, not a receiver result. The generic contract,
  injected transport boundary, strict output/failure handling, hidden-input separation,
  request/response hashes, call/token/cost/runtime caps, exact cache/replay, and six capability
  checks are implemented. No provider/model/local artifact is selected; no adapter or external
  call ran; mock certificates are exactly `infrastructure_only_non_evidence`.
- Route C is a fixed-contract synthetic/offline checkpoint. Exact scenario/configuration bytes,
  within-family scenario indices and seed derivation, Model-C payload, support-mask root,
  receiver/artifact/source hashes, policies, named streams, row order, output schema, and
  intent-to-treat admission reconstruct. The exact 3,600-row design remains unresolved among
  three prospective factorizations. Both production authorization pins remain `None`.
- Fresh hostile audit: all fixed-contract attacks rejected after two repair rounds, including
  coherent same-count scenario replacement, arbitrary scenario-stream hash, changed Model C and
  eligibility, receiver/provenance conflict, support and policy rewrites, RNG aliasing, malformed
  typed output, dependency changes, row reordering, exclusions, counts, roles, arms, and
  synthetic-to-production relabeling. Evidence class: independent audit, not outcome.
- Integrated regression command: eight baseline/agent/receiver/manifest/evaluator/report modules,
  `74` tests passed, `0` failed, wall time `3.76s`. The evaluator values and receiver responses in
  tests are synthetic canaries, not a payoff study.

> R0/Model B is quarantined after failed exact out-of-fold validation. It is not an authorized
> dependency of the current baseline, e-process, evaluator, receiver, or competition promotion
> gate. Its artifacts and failed routes are preserved for provenance.

Reopen Model B only through a new, explicitly authorized campaign with a new falsifiable
formulation, prespecified untouched validation, a strict resource budget, and a clear explanation
of what downstream decision would change if it passed. Do not simply resume the interrupted
actor-factor fit.

## Wave 5B Model-A necessity audit — prospective contract

Declared before extracting or scoring any Wave 5B residual. This is an architecture/feasibility
audit, not a Model-A fit and not a competition or factorial outcome.

- **Operational comparator.** Use the currently deployable schema-v1 independent-marginal
  opponent population at
  `/Users/sahararora/Glee-Compeition/models/opponent_population/opponent_population.json`.
  The retracted schema-v2/Model-B artifacts are prohibited. Integrate the v1 policy prediction
  over 4,096 deterministic draws (seed `20260816`) and use the corrected per-own-offer time unit.
- **Released evidence.** Score only actions whose acting model is in the existing deterministic
  25% model holdout. The current v1 artifact was fitted on all released data, so this is a
  conservative operational-misfit audit, not an independent validation claim. Report this
  contamination prominently. No post-outcome field may enter a prediction.
- **Live evidence.** Use exactly the terminal-complete `confirmation_20260815`,
  `volume_20260815`, and `volume2_20260815` logs. Evaluate only the visible opponent actions in
  games played by the frozen Jordan production agent. The launch manifests do not record a git
  commit, so live evidence is attributable to the agent class/run but not to an exact commit;
  this is a mandatory limitation, not an imputation.
- **Endpoints.** For every supported family/role/channel report count, log loss, Brier score,
  mean predicted probability, observed rate and calibration-in-the-large. For bargaining and
  negotiation offers report normalized MAE, bias and central-80% predictive coverage. Report
  successive-own-offer concession error and observed versus configured terminal round. Report
  family, role, complete-information, horizon, message-mode/history-length and live/offline
  slices when cell support is at least 30; cells are never pooled to conceal a failed role.
- **Coverage.** A discrete cell is interpretable at `n>=200` released or `n>=30` live; an offer
  cell at `n>=100` released or `n>=20` live. Unsupported action/termination channels are explicit
  zero-coverage defects, not silently dropped observations. Text persuasion stance is scored
  only when the frozen parser is unambiguous; otherwise it is reported as unknown.
- **Advance rule.** A development Model A is warranted only if all three hold: (1) a released
  cell shows either absolute calibration error `>=.05`, central-80% coverage below `.70`, or
  normalized offer MAE `>=.08`; (2) the same mechanism/direction appears in an eligible live
  cell or in both acting-model roles on released data; and (3) the defect occurs on an opponent
  branch actually observed in Jordan live games and names a simulator decision or candidate
  verdict that could change. Sampling error is summarized by game-cluster bootstrap intervals;
  no threshold or subgroup changes after results. If any condition fails, record exactly
  `deferred_no_demonstrated_incremental_need`.
- **Maximum status.** Any future small implementation remains
  `candidate_pending_independent_structural_validation`. It cannot supply the simulator,
  promotion gate, factorial baseline, or live policy. A full campaign must freeze actor/config
  folds, role-visible features, proper scores, coverage/calibration gates, resource limits and an
  independent hostile audit before fitting.

## Wave 5B shared-backbone benchmark — prospective contract

Declared before payoff simulation. This is a bounded architecture comparison, not a policy
promotion gate or a factorial treatment outcome.

- Compare the current production `my_agents.jordan_strategic:MyAgent` with the frozen
  treatment-off research backbone
  `research.CANDIDATES.wave3_factorial_agents:Factorial00Agent` on exactly `900` paired
  scenarios, seed `20260816`, round-robin balanced across bargaining, negotiation and
  persuasion.
- Use only the non-Model-B model-holdout marginal population SHA
  `3371131752a83dde73adba341aec78aab14af9bfd02b94b0bf7b773c317e051d`, config catalogue
  SHA `2a32c01dd02f08663c54b9f149c7fc8eda9e0d3b4ade59a0d463c0c55b67e2ae`, Model-C SHA
  `9daec869b3e4950945a1a370486e8841874fe9f5e611a7e8638dcdaa2b08b82c`, and support-index
  SHA `b958776534e764ed14099f4194af5437d4b9f8d6e5be3ec6537276eb133d9145`.
- Report paired mean difference, wins/losses/ties and family/role cells. No threshold is used to
  promote either policy. The benchmark only answers whether selecting Factorial00 as a live
  canary would preserve the currently deployed economic behavior closely enough to warrant a
  separate authorized test; it cannot estimate either factorial treatment.
