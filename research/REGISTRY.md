# Research route registry

| Route ID | Exact claim or mechanism | Current owner | Exact remaining gap | Smallest known obstruction | Next decisive test | Dependencies | Input revisions and evidence links | Route state | Evidence maturity | Stall classification | Reopen-only-if condition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R0 | Bounded Model B: the already-frozen mixed-fold joint opponent sampler predicts held-out joint behavior better than conditional independence. | root | None for the frozen formulation; its required run is complete. | Joint dependence worsened energy in B/N on both axes and persuasion retention was .330520. | No further Model-B formulation is authorized. Freeze the negative certificate. | Frozen mixed-fold artifacts and validator. | Competition `docs/REGISTRY.md`; report SHA `1a86cac...acec`; `research/FAILED.md`. | closed | verifier-backed | evidence against mechanism | User explicitly reopens R0 with a materially new scope. |
| R1 | A single theory-plus-validated-residual economic core is the shared baseline for four forced research entrypoints. | root | No economic-core gap remains at this checkpoint; payoff remains prohibited by the absent language environment/production manifest. | All four arms now fail without the exact Model-C and support artifacts, verify their bytes, expose identical provenance, and reject a mismatched arm. | Preserve this contract; any environment or economic change requires a new version and full parity. | R0 negative; shipped theory anchor; accepted Model C/support; non-Model-B holdout marginal population/catalogue. | `research/EVIDENCE/WAVE4_BASELINE_CONTRACT.json`; artifact manifest SHA `620e0b10...538e`; Wave 3 commit `a1438d0878dd89fff7d71accc7ddc7faebac983b`. | frozen | self-audited plus independent R4 baseline sub-audit | downstream environment dependency | Shared core, artifact bytes, population/catalogue, or receiver contract changes. |
| R2 | The exact treatment is a model-relative persuasion-seller e-process against a fixed hash-locked Model-C reference. | root | No implementation gap for this label; empirical payoff is deliberately unmeasured. | The sharp distribution-free future conditional upper bound from training data is `1`, so a nontrivial population-valid extension is impossible without extra assumptions. Bargaining/negotiation remain callback-censored and persuasion buyer is a self-outcome. | None in this wave; report this scope if/when an authorized factorial environment exists. | R1 core; Model C SHA `9daec869...b82c`; no Model B. | `research/ROUTES/R2_POPULATION_CLAIM_SETTLEMENT.md`; `research/EVIDENCE/R2_POPULATION_BOUND_KILL_CHECK.json`; `2/2` tests. | settled | theorem-strength kill check; treatment still self-audited | population premise killed | A prospectively justified population conditional bound and multiplicity design; never Model B or heuristic `E_*`. |
| R3 | A fixed language treatment changes only eligible persuasion-seller text while preserving the shared economic action. | root | User selection of a text-responsive environment; current offline eligible language population is empty. | Candidate text is delivered but ignored by all frozen offline receiver policies. Wave 5A now supplies a generic contract/capability/cache harness but project evidence supports no named receiver. | User supplies the 13 frozen receiver/selection/adapter/capability inputs in `WAVE5A_RECEIVER_DECISION.md`; only then run a generic capability certificate under separate authorization. | R1 core; dedicated language capability; no e-process or Model-B dependency. | `research/ROUTES/R3_LANGUAGE_DECISION.md`; `research/ROUTES/WAVE5A_RECEIVER_DECISION.md`; `tests/test_controlled_receiver.py`. | decision_pending | offline infrastructure unit/synthetic tested; no causal outcome evidence | causal identification/user selection | Explicit treatment-blind receiver selection, exact contract, generic untouched probes, adapter, caps, and external-call authorization. |
| R4 | The four forced arms and report verifier fail closed unless exact paired identities, immutable eligibility, capabilities, artifacts, and a hash-authorized 3,600-row production contract reconstruct. | root | Intentional obstruction: exact receiver and 3,600-row breadth/replication factorization are unselected; both report and pre-outcome authorization hashes are `None`. | Wave 5A builds exact scenario/Model-C/support/policy/RNG/output admission infrastructure and a fresh auditor rejects the fixed-contract hostile matrix, but no production row root exists. | Select receiver plus Design A/B/C, freeze exact rows and both pins, then obtain a new fresh audit of the activated version before any outcome. | R1 baseline; revision-2 estimands; controlled receiver contract; no Model B. | Report SHA `23cdbe69...f270`; `glee_eval/experiments/preoutcome_manifest.py`; `research/ROUTES/WAVE5A_MANIFEST_DESIGNS.md`; `tests/test_preoutcome_manifest.py`. | blocked_by_exact_obstruction | verifier-backed synthetic/offline checkpoint; no outcome evidence | receiver/design/authorization | User selects receiver and factorization; exact row root and two pins freeze; fresh activated audit passes. |
| R5 | The unrestricted competition champion integrates only confirmed components and is independently live-safe. | root competition owner | Reconcile dirty competition branch, freeze champion revision, and resolve exact one-run leaderboard command/cap. | Research routes must not alter the champion or consume its live authorization. | Resume the separate competition audit now that Wave 1 is durably synthesized; do not wait on research significance to bank authorized volume. | Existing competition gates/confirmations; live strict-cap adapter. | Competition worktree and `docs/REGISTRY.md`. | paused | candidate | orchestration separation | Resume after the Wave-1 durable commit; never import unfrozen research treatments. |

## Wave 5A offline-preflight checkpoint

- Route A is complete at `research/TECHNICAL_HANDOFF_2X2.md`. It is researcher-facing and
  source-grounded; it does not promote an empirical treatment claim.
- Route B is an implementation checkpoint, not a receiver result. The generic contract,
  injected transport boundary, strict output/failure handling, hidden-input separation,
  request/response hashes, call/token/cost/runtime caps, exact cache/replay, and six capability
  checks are implemented. No provider/model/local artifact is selected; no adapter or external
  call ran; mock certificates are exactly `infrastructure_only_non_evidence`.
- Route C is a fixed-contract synthetic/offline checkpoint. Exact scenario/configuration bytes,
  within-family scenario indices and seed derivation, Model-C payload, support-mask root,
  receiver/artifact/source hashes, policies, named streams, row order, output schema, and
  intent-to-treat admission reconstruct. The exact 3,600-row design remains unresolved among
  three prospective factorizations. Both production authorization pins remain `None`.
- Fresh hostile audit: all fixed-contract attacks rejected after two repair rounds, including
  coherent same-count scenario replacement, arbitrary scenario-stream hash, changed Model C and
  eligibility, receiver/provenance conflict, support and policy rewrites, RNG aliasing, malformed
  typed output, dependency changes, row reordering, exclusions, counts, roles, arms, and
  synthetic-to-production relabeling. Evidence class: independent audit, not outcome.
- Integrated regression command: eight baseline/agent/receiver/manifest/evaluator/report modules,
  `74` tests passed, `0` failed, wall time `3.76s`. The evaluator values and receiver responses in
  tests are synthetic canaries, not a payoff study.

> R0/Model B is quarantined after failed exact out-of-fold validation. It is not an authorized
> dependency of the current baseline, e-process, evaluator, receiver, or competition promotion
> gate. Its artifacts and failed routes are preserved for provenance.

Reopen Model B only through a new, explicitly authorized campaign with a new falsifiable
formulation, prespecified untouched validation, a strict resource budget, and a clear explanation
of what downstream decision would change if it passed. Do not simply resume the interrupted
actor-factor fit.
