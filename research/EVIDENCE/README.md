# Evidence records

Small durable certificates and hashes for large external artifacts live here. Large logs may
remain outside git, but every claim must link an immutable summary, reproduction command, and
input/output hashes.
