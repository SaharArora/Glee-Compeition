# R4 Wave 4 — fresh hostile report-verifier audit

Verdict: **GO for a verifier-backed exact obstruction; NO-GO for any production/payoff
study.**

Version bound:

- `glee_eval/experiments/factorial_report.py` SHA256
  `23cdbe690170b1d7eb598590e43e9bd7833d013019903b6acda7899e3455f270`;
- `glee_eval/experiments/factorial.py` SHA256
  `c272d602cafced4c93be00b43d4d7df7174229a543eda6ee08ec59dd95c4aee1`;
- research question revision 2 SHA256
  `221755b0b623d325143383e508b9b4132580b423134ef5ff15529d702c06ef45`.

The exact obstruction is executable: `AUTHORIZED_PRODUCTION_CONTRACT_SHA256` is `None`.
`FactorialReportContract.validate_production_freeze()` therefore rejects every production
contract before artifacts, outcomes, or report arithmetic are inspected. This is deliberate.
The current frozen offline receiver contract delivers candidate text but does not consume it, so
revision 2's primary language and interaction populations are empty/nonreportable. A production
pin cannot be supplied until the user selects a certified text-responsive receiver environment
and the resulting 3,600-row pre-outcome manifest, code, artifacts, command, population, and
catalogue are frozen.

The fresh isolated auditor re-executed all six previously accepted hostile mutations:

1. outcome-selected eligibility receives only explicitly synthetic/nonproduction status;
   production eligibility is recomputed from scenario, Model C, and receiver capability;
2. cross-owner RNG seed aliasing and arm-dependent economic traces are rejected;
3. changed `episode.opponent_spec` and changed realized nature evidence are rejected;
4. invented balanced role names are rejected against the exact family-role map;
5. a six-row contract cannot receive production validation; and
6. a plausible forged 3,600-row production contract is rejected while the authorization pin is
   absent.

The structurally eligible e-process headline is intentional under the user-authorized revision-2
question; overall contrasts remain mandatory secondary estimates. The auditor found no remaining
fixture that receives production evidence status.

Verification: `40/40` targeted report, evaluator, and real-agent tests passed. The independent
audit was read-only. No payoff row, Model B route, external model call, or live/rated game ran.

Any later change from a `None` production pin to an authorized contract is a new auditable
version. It requires a fresh independent audit of that exact contract and 3,600-row validation
path before payoff evaluation.
